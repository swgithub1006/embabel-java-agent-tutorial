spring:
  application:
    name: embabel-java-agent-tutorial

embabel:
  models:
    default-llm: deepseek-chat
  agent:
    platform:
      models:
        deepseek:
          api-key: ${DEEPSEEK_API_KEY}
