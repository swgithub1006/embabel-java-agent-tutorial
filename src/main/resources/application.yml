spring:
  application:
    name: embabel-java-agent-tutorial
  ai:
    ollama:
      # Not needed when using the default port
      base-url: http://192.168.3.66:11434

embabel:
  models:
    default-llm: qwen3:4b
  agent:
    platform:
      models:
        openai:
          custom:
            base-url: http://192.168.3.66:8000
            completions-path: /v3/chat/completions
            api-key: unused
            models: qwen3:4b


openweather:
  api:
    key: ${OPENWEATHER_API_KEY}
